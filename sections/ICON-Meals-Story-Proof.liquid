{% comment %}
  File: sections/iconmeals-story-proof.liquid
{% endcomment %}
<section class="icon-story-section">
  <div class="story-inner">
    <h2 class="story-headline">{{ section.settings.headline | default: "“It’s more affordable than buying groceries.”" }}</h2>

    <div class="story-photo-stack">
      {% if section.settings.image_1 != blank %}
        <img src="{{ section.settings.image_1 | img_url: 'master' }}" class="photo-left" alt="Meal stack photo">
      {% endif %}
      {% if section.settings.image_2 != blank %}
        <img src="{{ section.settings.image_2 | img_url: 'master' }}" class="photo-center" alt="Unboxing ICON Meals">
      {% endif %}
      {% if section.settings.image_3 != blank %}
        <img src="{{ section.settings.image_3 | img_url: 'master' }}" class="photo-right" alt="Meal layout photo">
      {% endif %}
    </div>

    <div class="story-body">
      <p>{{ section.settings.paragraph_1 | default: "Before ICON Meals, I was hitting the grocery store a few times a week. I’d buy stuff to cook lunch and dinner, thinking I was being smart — but I’d still end up spending more than I wanted, and wasting a lot of time." }}</p>
      <p>{{ section.settings.paragraph_2 | default: "Between cooking, cleaning, and tossing spoiled leftovers, it just felt like a lot of effort for not much payoff." }}</p>
      <p>{{ section.settings.paragraph_3 | default: "Now I grab a meal from the fridge, heat it up, and I’m eating in minutes. It’s consistent, it’s clean, and it fits how I actually live." }}</p>
      <p>{{ section.settings.paragraph_4 | default: "I ran the numbers — I’m saving about $8 a week compared to what I was spending on groceries for lunches and dinners. And I’m not stuck cleaning pans or wondering what to cook every night." }}</p>
      <p>{{ section.settings.paragraph_5 | default: "It’s been a small switch with a big impact. Didn’t expect that." }}</p>
      <div class="story-author">RJ / Real Customer<br><span class="story-location">Dallas, TX</span></div>
    </div>
  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Permanent+Marker&family=Inter:wght@400;600;700&display=swap');

  .icon-story-section {
    background: #fffefc;
    padding: 60px 24px;
    border-top: 1px solid #eee;
    border-bottom: 1px solid #eee;
  }

  .story-inner {
    max-width: 760px;
    margin: 0 auto;
  }

  .story-headline {
    font-family: 'Permanent Marker', cursive;
    font-size: 2.3rem;
    color: #2b2b2b; /* dark charcoal */
    text-align: center;
    margin-bottom: 2.4em;
    line-height: 1.3;
  }

  .story-photo-stack {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    position: relative;
    margin-bottom: 2.5em;
    flex-wrap: nowrap;
    gap: 12px;
  }

  .story-photo-stack img {
    width: 33%;
    border-radius: 14px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
  }

  .photo-left {
    z-index: 1;
    transform: rotate(-3deg) scale(0.96);
  }

  .photo-center {
    z-index: 3;
    transform: rotate(1deg) scale(1.05);
    box-shadow: 0 8px 24px rgba(0,0,0,0.15);
  }

  .photo-right {
    z-index: 2;
    transform: rotate(3deg) scale(0.96);
  }

  .story-body {
    text-align: left;
  }

  .story-body p {
    font-family: 'Inter', sans-serif;
    font-size: 1.1rem;
    line-height: 1.8;
    color: #222;
    margin-bottom: 1.4em;
  }

  .story-author {
    font-family: 'Inter', sans-serif;
    font-size: 1rem;
    font-weight: 600;
    color: #444;
    line-height: 1.4;
  }

  .story-location {
    font-weight: 500;
    font-size: 0.95rem;
    opacity: 0.8;
  }

  @media (max-width: 767px) {
    .story-photo-stack {
      flex-direction: row;
      gap: 8px;
      justify-content: center;
    }

    .story-photo-stack img {
      width: 32%;
      transform-origin: center center;
    }

    .photo-left {
      transform: rotate(-5deg) scale(0.94);
    }

    .photo-center {
      transform: rotate(0deg) scale(1.05);
    }

    .photo-right {
      transform: rotate(4deg) scale(0.94);
    }

    .story-headline {
      font-size: 1.6rem;
      margin-bottom: 2em;
    }

    .story-body p {
      font-size: 1rem;
    }
  }
</style>

{% schema %}
{
  "name": "ICON Meals Story Proof",
  "settings": [
    {
      "type": "text",
      "id": "headline",
      "label": "Headline",
      "default": "“It’s more affordable than buying groceries.”"
    },
    {
      "type": "textarea",
      "id": "paragraph_1",
      "label": "Paragraph 1",
      "default": "Before ICON Meals, I was hitting the grocery store a few times a week. I’d buy stuff to cook lunch and dinner, thinking I was being smart — but I’d still end up spending more than I wanted, and wasting a lot of time."
    },
    {
      "type": "textarea",
      "id": "paragraph_2",
      "label": "Paragraph 2",
      "default": "Between cooking, cleaning, and tossing spoiled leftovers, it just felt like a lot of effort for not much payoff."
    },
    {
      "type": "textarea",
      "id": "paragraph_3",
      "label": "Paragraph 3",
      "default": "Now I grab a meal from the fridge, heat it up, and I’m eating in minutes. It’s consistent, it’s clean, and it fits how I actually live."
    },
    {
      "type": "textarea",
      "id": "paragraph_4",
      "label": "Paragraph 4",
      "default": "I ran the numbers — I’m saving about $8 a week compared to what I was spending on groceries for lunches and dinners. And I’m not stuck cleaning pans or wondering what to cook every night."
    },
    {
      "type": "textarea",
      "id": "paragraph_5",
      "label": "Paragraph 5",
      "default": "It’s been a small switch with a big impact. Didn’t expect that."
    },
    {
      "type": "image_picker",
      "id": "image_1",
      "label": "Left photo"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Center photo (hero)"
    },
    {
      "type": "image_picker",
      "id": "image_3",
      "label": "Right photo"
    }
  ],
  "presets": [
    {
      "name": "Visual Story Stack",
      "category": "Custom"
    }
  ]
}
{% endschema %}
