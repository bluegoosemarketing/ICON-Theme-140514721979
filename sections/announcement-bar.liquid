{{ 'section-announcement-bar.min.css' | asset_url | stylesheet_tag }}

{% comment %}
  ========================================
  VISIBILITY LOGIC
  ========================================
{% endcomment %}

{% assign show_announcement = false %}

{% if template.name == 'index' %}
  {% assign show_announcement = true %}
{% elsif template.name == 'page' %}
  {% if page.handle != 'add-ons' %}
    {% assign show_announcement = true %}
  {% endif %}
{% elsif template.name == 'blog' or template.name == 'article' %}
  {% assign show_announcement = true %}
{% elsif request.path contains '/apps/help-center' %}
  {% assign show_announcement = true %}
{% endif %}

{% if section.settings.enable and show_announcement %}

<style>
  /* 
    ========================================
    THEME: BRUSHED BULLION (REALISTIC GOLD)
    Aesthetic: Vertical Gradient, Texture, 3D Volume
    ========================================
  */

  :root {
    /* 24K GOLD PALETTE */
    --im-gold-light: #F9EFAF; /* Top Highlight */
    --im-gold-mid: #D4AF37;   /* The Core Gold */
    --im-gold-shadow: #AA771C; /* Bottom Shadow */
    
    --im-text-ink: #111111;
    --im-brand-red: #D11F1F;
    --im-pure-white: #ffffff;
  }

  /* 1. CONTAINER & RESET */
  a.im-ann-bar-root {
    position: relative !important;
    width: 100% !important;
    min-height: 44px !important;
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
    text-decoration: none !important;
    z-index: 50 !important;
    cursor: pointer !important;
    overflow: hidden !important;
    font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif !important;
    color: var(--im-text-ink) !important;

    /* SATIN GOLD GRADIENT (Subtle & Smooth) */
    background: linear-gradient(
      180deg, 
      #E6D699 0%, 
      #D4C179 50%, 
      #C2AE5E 100%
    ) !important;
    
    /* SOFTENED EDGES */
    border-top: 1px solid rgba(255,255,255,0.25) !important;
    border-bottom: 1px solid rgba(0,0,0,0.1) !important; 
  }

  /* 2. BRUSHED METAL TEXTURE OVERLAY */
  a.im-ann-bar-root::before {
    content: "" !important;
    position: absolute !important;
    top: 0; left: 0; right: 0; bottom: 0;
    /* Faint diagonal scratches */
    background-image: repeating-linear-gradient(
      45deg,
      rgba(255,255,255,0) 0px,
      rgba(255,255,255,0) 2px,
      rgba(255,255,255,0.05) 3px,
      rgba(255,255,255,0) 4px
    ) !important;
    pointer-events: none !important;
    z-index: 1 !important;
  }

  /* 3. SHIMMER ANIMATION (Subtle sheen passing over) */
  a.im-ann-bar-root::after {
    content: "" !important;
    position: absolute !important;
    top: 0; left: -100%;
    width: 50%; height: 100%;
    background: linear-gradient(
      90deg, 
      transparent, 
      rgba(255,255,255,0.4), 
      transparent
    ) !important;
    transform: skewX(-20deg) !important;
    animation: imSheen 6s infinite linear !important;
    pointer-events: none !important;
    z-index: 2 !important;
  }

  @keyframes imSheen {
    0% { left: -50%; opacity: 0; }
    20% { opacity: 1; }
    40% { left: 150%; opacity: 0; }
    100% { left: 150%; opacity: 0; }
  }

  /* 2. NUCLEAR TEXT RESET */
  .im-ann-bar-root *, .im-ann-bar-root span {
    text-decoration: none !important;
    line-height: 1 !important;
    box-sizing: border-box !important;
  }

  /* 3. LAYOUT */
  .im-ann-content {
    position: relative !important;
    z-index: 5 !important; /* Sit above texture */
    display: flex !important;
    align-items: center !important;
    gap: 16px !important;
    padding: 0 15px !important;
  }

  .im-group-left, .im-group-right {
    display: flex !important;
    align-items: center !important;
  }
  .im-group-left { gap: 10px !important; }
  .im-group-right { gap: 8px !important; }

  /* 4. THE RED PILL BADGE */
  .im-badge-pill {
    background-color: var(--im-brand-red) !important;
    color: #fff !important;
    font-weight: 800 !important;
    font-size: 12px !important;
    padding: 5px 10px !important;
    border-radius: 4px !important; 
    box-shadow: 0 2px 5px rgba(0,0,0,0.15) !important;
    letter-spacing: 0.5px !important;
    position: relative !important;
    white-space: nowrap !important;
    text-transform: uppercase !important;
    border: 1px solid rgba(0,0,0,0.1) !important;
  }

  /* 5. TEXT STYLES */
  .im-ann-text {
    font-weight: 700 !important;
    font-size: 13px !important;
    text-transform: uppercase !important;
    letter-spacing: 1px !important;
    color: var(--im-text-ink) !important;
    white-space: nowrap !important;
    text-shadow: 0 1px 0 rgba(255,255,255,0.3) !important; /* Engraved look */
  }

  .im-ann-label {
    font-size: 11px !important;
    color: #333 !important; 
    text-transform: uppercase !important;
    font-weight: 700 !important;
    opacity: 0.85 !important;
  }

  /* 6. THE TICKET */
  .im-code-ticket {
    background: var(--im-pure-white) !important;
    border: 1px dashed #8E6316 !important; /* Dark bronze stitch */
    color: #000000 !important;
    font-family: "Courier New", Courier, monospace !important;
    font-weight: 800 !important;
    font-size: 14px !important;
    padding: 4px 10px !important;
    border-radius: 4px !important;
    letter-spacing: 0.5px !important;
    position: relative !important;
    box-shadow: 0 1px 2px rgba(0,0,0,0.1) !important;
  }
  
  /* Ticket "Notches" - Color matched to the mid-tone gold */
  .im-code-ticket::before, .im-code-ticket::after {
    content: '' !important;
    position: absolute !important;
    height: 8px !important;
    width: 8px !important;
    background-color: #D4AF37 !important; /* Matches gold background */
    border-radius: 50% !important;
    top: 50% !important;
    transform: translateY(-50%) !important;
    z-index: 2 !important;
    box-shadow: inset 0 1px 2px rgba(0,0,0,0.1) !important;
  }
  
  .im-code-ticket::before { left: -5px !important; }
  .im-code-ticket::after { right: -5px !important; }

  /* 7. GIFT ICON */
  .im-icon-gift {
    width: 16px !important;
    height: 16px !important;
    margin-left: 2px !important;
    color: #000 !important;
    opacity: 0.8 !important;
  }

  /* 
     ========================================
     8. MOBILE OPTIMIZATION
     ========================================
  */
  @media (max-width: 767px) {
    a.im-ann-bar-root {
      min-height: 40px !important;
      height: 40px !important;
      padding: 0 8px !important;
    }
    
    .im-ann-content {
      gap: 10px !important;
      width: 100% !important;
      justify-content: center !important;
    }

    .im-group-left { gap: 8px !important; }
    .im-group-right { gap: 6px !important; }

    .im-ann-label { display: none !important; }
    
    .im-badge-pill { 
      font-size: 10px !important; 
      padding: 3px 6px !important; 
    }
    
    .im-ann-text { 
      font-size: 11px !important; 
    }
    
    .im-code-ticket { 
      font-size: 12px !important; 
      padding: 2px 6px !important; 
    }
    
    .im-code-ticket::before, .im-code-ticket::after {
      height: 6px !important; width: 6px !important;
    }
    .im-code-ticket::before { left: -3px !important; }
    .im-code-ticket::after { right: -3px !important; }
  }
</style>

<!-- 
  LINK: Auto-apply ELF25 
-->
<a href="/discount/ELF25?redirect=/collections/signature-menu" class="im-ann-bar-root">
  
  <div class="im-ann-content">
    
    <!-- Left: Red Pill Badge + Text -->
    <div class="im-group-left">
      <div class="im-badge-pill">
        <span>25% OFF</span>
      </div>
      <span class="im-ann-text">HOLIDAY SALE</span>
    </div>

    <!-- Right: Ticket + Icon -->
    <div class="im-group-right">
      <span class="im-ann-label">USE CODE:</span>
      
      <!-- The White Cutout Ticket -->
      <div class="im-code-ticket">
        ELF25
      </div>
      
      <!-- Gift Icon -->
      <svg class="im-icon-gift" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="20 12 20 22 4 22 4 12"></polyline>
        <rect x="2" y="7" width="20" height="5"></rect>
        <line x1="12" y1="22" x2="12" y2="7"></line>
        <path d="M12 7H7.5a2.5 2.5 0 0 1 0-5C11 2 12 7 12 7z"></path>
        <path d="M12 7h4.5a2.5 2.5 0 0 0 0-5C13 2 12 7 12 7z"></path>
      </svg>
    </div>

  </div>

</a>

<script>
  console.log('IM Holiday Bar: Brushed Bullion Loaded');
</script>

{% endif %}

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    {
      "type": "checkbox",
      "id": "enable",
      "default": true,
      "label": "Show announcement"
    }
  ]
}
{% endschema %}